# API credentials — never commit these
config.yaml

# Patent cache (inventors + co-assignees) — stays local
patent_cache.json
inventor_cache.json

# Proxy list
proxies.txt

# Output directories
search_*/

# Python
__pycache__/
*.pyc
*.pyo
.env

# OS
.DS_Store
Thumbs.db
