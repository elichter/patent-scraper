# API credentials — never commit these
config.yaml

# Patent cache (inventors + co-assignees) — stays local
patent_cache.json
inventor_cache.json

# Monthly usage tracker — stays local
usage_tracker.json

# Proxy list
proxies.txt

# Output directories
search_*/

# Python
__pycache__/
*.pyc
*.pyo
.env

# Local reference files
CHANGELOG_SETUP.md
GIT_WORKFLOW.md

# OS
.DS_Store
Thumbs.db
